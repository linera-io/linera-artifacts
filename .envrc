# direnv config for linera-io/artifacts.
#
# direnv walks up the filesystem looking for a .envrc, so this single
# file activates the flake devshell (see flake.nix) in the repo root
# AND in every subdirectory automatically.
#
# First-time setup: `direnv allow` in the repo root.

use flake
