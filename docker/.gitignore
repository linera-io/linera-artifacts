.env
.env.backup.*
docker-compose.override.yml
server.json
genesis.json
committee.json
wallet.json
keystore.json
linera.db/
*.backup.*

# Generated by deploy-validator.sh (per-validator hostname + shard count).
# Operators don't edit this — script regenerates on every deploy.
validator-config.toml
